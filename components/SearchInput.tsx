// Required for client component to use React hooks
"use client";

// SearchInput component
const SearchInput = () => {
  return (
    <div>
      Search Input
    </div>
  );
}

export default SearchInput;
